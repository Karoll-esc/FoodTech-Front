import type { Product } from '../models/Product';

/**
 * Datos de productos del menú gourmet (DEPRECADO)
 * Este archivo ya no se usa. Los productos se obtienen del backend mediante useProducts hook.
 * Se mantiene solo para compatibilidad con imports existentes.
 */
export const MENU_PRODUCTS: Partial<Product>[] = [];

